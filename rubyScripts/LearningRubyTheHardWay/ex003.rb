# prints a line with only text
puts "I will now count my chickens:"

# prints a line with some text and inserts a computation using #{}
puts "Hens #{25 + 30 / 6}"
# prints a line with some text and inserts a computation using #{}
puts "Roosters #{100.0 - 25.0 * 3.0 % 4.0}"

# prints a line with only text
puts "Now I will count the eggs:"

# prints the result of a computation
puts 3.0 + 2.0 + 1.0 - 5.0 + 4.0 % 2.0 - 1.0 / 4.0 + 6.0

# prints a line with only text
puts "Is it true that 3 + 2 < 5 - 7?"

# prints a boolean which is true or false depending on the condition
puts 3 + 2 < 5 - 7

# prints a line with some text and insert a computation using #{}
puts "What is 3 + 2? #{3 + 2}"
# prints a line with some text and insert a computation using #{}
puts "What is 5 - 7? #{5 - 7}"

# prints a line with only text
puts "Oh, that's why it's false."

# prints a line with only text
puts "How about some more."

# prints a line with some text and insert a computation using #{} that returns a boolean value
puts "Is it greater? #{5 > -2}"
# prints a line with some text and insert a computation using #{} that returns a boolean value
puts "Is it greater or equal? #{5 >= -2}"
# prints a line with some text and insert a computation using #{} that returns a boolean value
puts "Is it less or equal? #{5 <= -2}"